<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AmbientContextMcp.Settings.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" MinHeight="22" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Pivot Grid.Row="0">
            <PivotItem x:Name="McpServerTab">
                <Grid Padding="12" RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12">
                        <Grid RowSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="McpServerGroupText" Grid.Row="0" Grid.ColumnSpan="3" FontWeight="SemiBold" Margin="0,0,0,4" />

                            <TextBlock x:Name="LabelStatusText" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" x:Name="McpStatusText" VerticalAlignment="Center" />

                            <TextBlock x:Name="LabelEndpointText" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
                            <TextBox Grid.Row="2" Grid.Column="1" x:Name="McpEndpointBox" IsReadOnly="True" Margin="0,0,8,0" />
                            <Button x:Name="CopyEndpointButton" Grid.Row="2" Grid.Column="2" Click="OnCopyEndpointClick" MinWidth="64" />

                            <TextBlock x:Name="LabelTokenText" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" />
                            <TextBox Grid.Row="3" Grid.Column="1" x:Name="McpTokenBox" IsReadOnly="True" Margin="0,0,8,0" />
                            <Button x:Name="CopyTokenButton" Grid.Row="3" Grid.Column="2" Click="OnCopyTokenClick" MinWidth="64" />

                            <TextBlock x:Name="LabelPortText" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" />
                            <TextBox Grid.Row="4" Grid.Column="1" x:Name="McpPortBox" Margin="0,0,8,0" />
                            <TextBlock x:Name="PortChangeNoteText" Grid.Row="4" Grid.Column="2" VerticalAlignment="Center" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />

                            <CheckBox x:Name="McpAutoStartCheckBox" Grid.Row="5" Grid.ColumnSpan="3" Margin="0,4,0,0" />
                            <CheckBox x:Name="PersistEventLogCheckBox" Grid.Row="6" Grid.ColumnSpan="3" Margin="0,4,0,0" />
                            <TextBlock x:Name="PersistEventLogNoteText" Grid.Row="7" Grid.ColumnSpan="3" Margin="28,0,0,4" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />

                            <Button x:Name="CopySnippetButton" Grid.Row="8" Grid.ColumnSpan="3" Click="OnCopyClaudeCodeSnippetClick" HorizontalAlignment="Left" MinWidth="240" Margin="0,4,0,0" />
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="LabelLanguageText" Grid.Column="0" VerticalAlignment="Center" />
                        <ComboBox Grid.Column="1" x:Name="UiLanguageBox" SelectedValuePath="Tag" MinWidth="220" HorizontalAlignment="Left">
                            <ComboBoxItem x:Name="LanguageSystemDefaultItem" Tag="" />
                            <ComboBoxItem x:Name="LanguageJapaneseItem" Tag="ja" />
                            <ComboBoxItem x:Name="LanguageEnglishItem" Tag="en" />
                        </ComboBox>
                    </Grid>
                </Grid>
            </PivotItem>

            <PivotItem x:Name="TransmissionTab">
                <Grid Padding="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="TransmissionExplanationText" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10" />

                    <CheckBox x:Name="SelectAllTransmissionCheckBox"
                              Grid.Row="1"
                              Click="OnToggleAllTransmissionClick"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,8" />

                    <Border Grid.Row="2"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="10"
                            Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="24" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="EventHistoryGroupText" Grid.Row="0" Grid.ColumnSpan="5" FontWeight="SemiBold" Margin="0,0,0,4" />
                            <TextBlock x:Name="LabelRetentionText" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,4,8,0" />
                            <ComboBox x:Name="EventRetentionHoursBox" Grid.Row="1" Grid.Column="1" SelectedValuePath="Tag" MinWidth="130" Margin="0,4,0,0">
                                <ComboBoxItem x:Name="Retention1HourItem" Tag="1" />
                                <ComboBoxItem x:Name="Retention6HoursItem" Tag="6" />
                                <ComboBoxItem x:Name="Retention24HoursItem" Tag="24" />
                                <ComboBoxItem x:Name="Retention7DaysItem" Tag="168" />
                            </ComboBox>
                            <TextBlock x:Name="LabelMaxCountText" Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" Margin="0,4,8,0" />
                            <ComboBox x:Name="EventRetentionCountBox" Grid.Row="1" Grid.Column="4" SelectedValuePath="Tag" MinWidth="130" Margin="0,4,0,0">
                                <ComboBoxItem x:Name="Count100Item" Tag="100" />
                                <ComboBoxItem x:Name="Count500Item" Tag="500" />
                                <ComboBoxItem x:Name="Count1000Item" Tag="1000" />
                                <ComboBoxItem x:Name="Count5000Item" Tag="5000" />
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Grid.Row="3"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="TransmissionGroupsList" Margin="8,8,14,8">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="8,10,8,6" Margin="0,0,0,10">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                                <ItemsControl ItemsSource="{Binding Options}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Padding="4,7" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="88" />
                                                                </Grid.ColumnDefinitions>
                                                                <CheckBox Grid.Column="0"
                                                                          IsChecked="{Binding IsAllowed, Mode=TwoWay}"
                                                                          VerticalAlignment="Center"
                                                                          HorizontalAlignment="Left"
                                                                          MinWidth="0"
                                                                          Margin="0,0,10,0" />
                                                                <TextBlock Grid.Column="1"
                                                                           Text="{Binding Label}"
                                                                           TextWrapping="Wrap"
                                                                           VerticalAlignment="Center"
                                                                           Margin="0,0,12,0" />
                                                                <TextBlock Grid.Column="2"
                                                                           Text="{Binding Sensitivity}"
                                                                           HorizontalAlignment="Right"
                                                                           VerticalAlignment="Center"
                                                                           Margin="0,0,8,0" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <TextBlock x:Name="SensitivityLegendText"
                               Grid.Row="4"
                               Margin="0,8,0,0"
                               TextWrapping="Wrap"
                               FontSize="11"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                </Grid>
            </PivotItem>
        </Pivot>

        <TextBlock Grid.Row="1"
                   x:Name="SettingsStatusText"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"
                   Margin="0,10,0,4" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,4,0,0">
            <Button x:Name="SaveButton"
                    Click="OnSaveClick"
                    MinWidth="96"
                    Style="{ThemeResource AccentButtonStyle}" />
            <Button x:Name="CloseButton"
                    Click="OnCloseClick"
                    MinWidth="96" />
        </StackPanel>
    </Grid>
</Window>
